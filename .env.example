# OpenPlantbook API Authentication
#
# Get your free API credentials at: https://open.plantbook.io/apikey/show/
#
# IMPORTANT: Copy this file to .env and fill in your actual credentials
# The .env file is git-ignored and will NOT be committed to the repository
#
# Usage:
#   1. cp .env.example .env
#   2. Edit .env with your actual credentials (choose ONE authentication method)
#   3. For integration tests: go test -tags=integration ./...
#   4. For CLI usage: openplantbook search monstera

# ==============================================================================
# AUTHENTICATION - Choose ONE method:
# ==============================================================================

# Method 1: API Key Authentication (RECOMMENDED - Simpler)
# - Single credential
# - Never expires
# - Covers search and details endpoints (v1.0.0 scope)
OPENPLANTBOOK_API_KEY=your_api_key_here

# Method 2: OAuth2 Client Credentials (Full API Access)
# - Two credentials (client_id and client_secret)
# - Time-limited tokens (automatically refreshed)
# - Required for write operations (sensor data, user plants)
# NOTE: Comment out API_KEY above if using OAuth2
# OPENPLANTBOOK_CLIENT_ID=your_client_id_here
# OPENPLANTBOOK_CLIENT_SECRET=your_client_secret_here

# ==============================================================================
# Optional Configuration
# ==============================================================================

# Log level (debug, info, warn, error)
# OPENPLANTBOOK_LOG_LEVEL=info

# Disable caching for testing
# OPENPLANTBOOK_NO_CACHE=false

# Custom output format for CLI (json, yaml, table)
# OPENPLANTBOOK_FORMAT=table
